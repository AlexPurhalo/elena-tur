.row
  .col-md-12
    = image_tag @tour.cover.url(:large)
    %h1= @tour.title
    = simple_format(@tour.description)
    %p= @tour.price
    %p= @tour.date
    =link_to 'Назад', tours_path
    - if user_signed_in? && current_user.admin?
      =link_to 'Delete', tour_path, method: :delete
      =link_to 'Edit', edit_tour_path
