source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.6'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'

# Use jquery as the JavaScript library

gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks

gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder

gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.

gem 'sdoc', '~> 0.4.0', group: :doc

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'

  # Use sqlite3 as the database for Active Record
  gem 'sqlite3'
end

group :production do
  # Pg is the Ruby interface to the {PostgreSQL RDBMS}[http://www.postgresql.org/].
  # It works with {PostgreSQL 8.4 and later}[http://www.postgresql.org/support/versioning/].
  # A small example usage: #!/usr/bin/env ruby require 'pg' # Output a table
  # of current connections to the DB conn = PG.connect( dbname: 'sales' )
  # conn.exec( "SELECT * FROM pg_stat_activity" ) do |result| puts " PID |
  # User | Query" result.each do |row| puts " %7d | %-16s | %s " % row.values_at('procpid',
  # 'usename', 'current_query') end end
  gem 'pg', '0.18.1'

  # Run Rails the 12factor way
  gem 'rails_12factor'
end

# Haml (HTML Abstraction Markup Language) is a layer on top of HTML or XML that's designed to express the structure of
# documents in a non-repetitive, elegant, and easy way by using indentation rather than closing tags and allowing
# Ruby to be embedded with ease.
# It was originally envisioned as a plugin for Ruby on Rails, but it can function as a stand-alone templating engine.
gem 'haml', '~> 4.0', '>= 4.0.7'

# bootstrap-sass is a Sass-powered version of Bootstrap 3, ready to drop right into your Sass powered applications.
gem 'bootstrap-sass', '~> 3.3', '>= 3.3.6'

# Easy upload management for ActiveRecord
gem 'paperclip', '~> 4.3', '>= 4.3.6'

# Forms made easy!
gem 'simple_form', '~> 3.2', '>= 3.2.1'

# Flexible authentication solution for Rails with Warden
gem 'devise', '~> 4.1', '>= 4.1.1'

# A generalized Rack framework for multiple-provider authentication.
gem 'omniauth', '~> 1.3', '>= 1.3.1'

# A Google OAuth2 strategy for OmniAuth 1.x
gem 'omniauth-google-oauth2'

# Facebook OAuth2 Strategy for OmniAuth
gem 'omniauth-facebook', '~> 3.0'

# Unofficial VKontakte strategy for OmniAuth 1.0
gem 'omniauth-vkontakte', '~> 1.3', '>= 1.3.7'

# will_paginate provides a simple API for performing paginated queries with Active Record, DataMapper and Sequel,
# and includes helpers for rendering pagination links in Rails, Sinatra and Merb web apps.
gem 'will_paginate', '~> 3.1'

# The official AWS SDK for Ruby. Provides both resource oriented interfaces and API clients for AWS services.
gem 'aws-sdk', '< 2.0'